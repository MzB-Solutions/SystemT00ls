namespace SystemT00ls
{
    internal class FunctionsType
    {
        #region Internal Enums

        internal enum availableTools
        {
            nullTool = 0,
            adTools = 1,
            dockerTools = 2,
            computerTools = 3
        };

        internal enum menuType
        {
            nullMenu = 0,
            mainMenu = 1,
            adMenu = 2,
            dockerMenu = 3,
            compMenu = 4
        };

        #endregion Internal Enums
    }
}